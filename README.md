# Mood-Detection-AI
AI Mood Detection from Text is a Machine Learning–based web application that analyzes a user's text input and predicts the emotional mood behind it. The system uses Natural Language Processing (NLP) techniques to preprocess textual data and applies a trained machine learning model to classify the text
